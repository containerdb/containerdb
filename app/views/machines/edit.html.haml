= content_for :title, "Edit #{@machine.name}"

.row
  .col-md-3
    %h1.lead= @machine.name
    = render 'form'
  .col-md-9
    %h1.lead Services
    = render 'services/table', services: @machine.services
